version: "3"
services:
  express-app:
    image: voffch/edu-express-app:latest
    ports:
      - 127.0.0.1:8080:8080
  watchtower:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 60